# Backend Roadmap

## الهدف

بناء Backend آمن وقابل للتوسع يخدم الموقع العام ولوحة الإدارة، مع تسجيل العملاء المحتملين، إدارة البرامج والمحتوى، وتشغيل عجلة الهدايا مع منع تكرار أرقام الواتساب.

## ما تم إنجازه

- REST APIs منظمة ومتوثقة عبر Swagger UI.
- تسجيل دخول الأدمن بجلسة Bearer Token.
- CMS للمحتوى مع رفع الصور.
- Leads: إنشاء عام، بحث وفلترة وتعديل من الأدمن.
- Programs: عرض عام وإضافة وتعديل من الأدمن.
- Dashboard summary وAudit logs.
- Wheel APIs: spin وclaim.
- توحيد رقم الواتساب والتحقق من عدم تسجيله أكثر من مرة.
- السماح بطلبات PATCH من المتصفح.

## الحالة الحالية

- التخزين انتقل إلى SQLite (`server/data/app.db`) مع migration تلقائي من `content-store.json`.
- رقم الواتساب عليه unique index داخل قاعدة البيانات.
- يوجد smoke test قابل للتشغيل عبر `npm run test:api` ويختبر الصلاحيات ومنع التكرار وSwagger.
- الـ Admin Dashboard متصل بالـ API ويعرض الملخص والعملاء والبرامج ونتائج العجلة.
- Postman collection محدثة بمسارات العجلة.
- لوحة الإدارة أصبحت تعرض فقط الصفحات الموصولة فعليًا بالموقع الحالي، مع معاينة مباشرة قبل الحفظ.
- صفحات دفعة 2027 وقصص النجاح وFeedback أصبحت تقرأ نصوصها وقوائمها من CMS بدل القيم الثابتة.

## مراحل التنفيذ التالية

### المرحلة 1 — تثبيت الـ API Contract

- توحيد شكل الأخطاء والاستجابات في middleware واحد.
- إضافة اختبارات idempotency للعجلة ورفع الملفات بالتفصيل.

### المرحلة 2 — التخزين

- إضافة Repository methods متخصصة بدل read/replace الكامل لكل عملية.
- تجهيز PostgreSQL adapter عند الانتقال للاستضافة متعددة النسخ.

### المرحلة 3 — الأمان

- إجبار تغيير بيانات دخول الأدمن الافتراضية.
- CORS من قائمة Origins محددة بدل `*`.
- Rate limiting لتسجيل الدخول والعجلة والفورمات العامة.
- إخفاء تفاصيل الأخطاء في production.
- تنظيف وإدارة انتهاء الجلسات والتوكنات.

### المرحلة 4 — Admin Dashboard

- API client وInterceptor للتوكن في Angular.
- شاشة مؤشرات Dashboard.
- شاشة Leads مع البحث والفلترة وتغيير الحالة والملاحظات.
- شاشة Programs للإضافة والتعديل والتفعيل والإغلاق.
- شاشة Audit Logs كاملة مع pagination.
- شاشة إدارة إعدادات العجلة والهدايا.

### المرحلة 5 — الاختبارات والتشغيل

- اختبارات duplicate phone وwheel idempotency.
- اختبارات صلاحيات الأدمن.
- اختبارات رفع الصور وحجم ونوع الملف.
- اختبار build وproduction configuration.
- إعداد backup وhealth checks وlogging.

### المرحلة 6 — توسيع Content Modules

- تحويل معرض الصور، الأخبار، المدارس، المدرسين، وشروط التقديم إلى modules CMS فعلية.
- إضافة صلاحيات متعددة للأدمن وسجل تغييرات كامل لكل صفحة.
- إضافة Preview Draft قبل النشر ونسخ احتياطية/استرجاع للمحتوى.
